<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_app"
    style="@style/Widget.DualSpaceOBS.MaterialCard"
    android:layout_width="match_parent"
    android:layout_height="@dimen/item_app_height"
    android:layout_marginHorizontal="@dimen/margin_medium"
    android:layout_marginVertical="@dimen/margin_tiny"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="@dimen/corner_radius_medium"
    app:cardElevation="@dimen/elevation_card">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/padding_list_item">

        <!-- App Icon -->
        <ImageView
            android:id="@+id/iv_app_icon"
            style="@style/AppItemIcon"
            android:layout_marginEnd="@dimen/margin_medium"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:src="@mipmap/ic_launcher" />

        <!-- App Name -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/tv_app_name"
            style="@style/AppItemName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_tiny"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/iv_app_icon"
            app:layout_constraintEnd_toStartOf="@id/btn_clone_app"
            tools:text="WhatsApp" />

        <!-- Package Name -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/tv_package_name"
            style="@style/AppItemPackageName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/tv_app_name"
            app:layout_constraintStart_toEndOf="@id/iv_app_icon"
            app:layout_constraintEnd_toStartOf="@id/btn_clone_app"
            tools:text="com.whatsapp" />

        <!-- App Size -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/tv_app_size"
            style="@style/AppItemSize"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/tv_package_name"
            app:layout_constraintStart_toEndOf="@id/iv_app_icon"
            tools:text="45.2 MB" />

        <!-- Clone Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_clone_app"
            style="@style/Widget.DualSpaceOBS.Button.Icon.Filled"
            android:layout_width="@dimen/icon_button_size"
            android:layout_height="@dimen/icon_button_size"
            android:backgroundTint="@color/accent_600"
            app:icon="@drawable/ic_clone"
            android:contentDescription="@string/btn_clone"
            app:iconSize="@dimen/icon_medium"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
