name: Build Android APK

on:
  push:
    branches: [main, master]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".gitignore"
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      create_release:
        description: "Create a GitHub release after build"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write

env:
  JAVA_DISTRIBUTION: temurin
  JAVA_VERSION: "17"

jobs:
  # ──────────────────────────────────────────────
  # Job 1: Validate Gradle wrapper before anything
  # ──────────────────────────────────────────────
  gradle-validation:
    name: Validate Gradle Wrapper
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v4

  # ──────────────────────────────────────────────
  # Job 2: Build APKs across a matrix of API levels
  # ──────────────────────────────────────────────
  build:
    name: Build APK (API ${{ matrix.api-level }}, ${{ matrix.build-type }})
    needs: gradle-validation
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        api-level: [30, 33, 34]
        build-type: [debug, release]
        include:
          - api-level: 34
            build-type: release
            is_latest: true

    steps:
      # ── 1. Checkout ──
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Set up JDK ──
      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}

      # ── 3. Cache Gradle ──
      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-api${{ matrix.api-level }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-api${{ matrix.api-level }}-
            gradle-${{ runner.os }}-

      # ── 4. Grant execute permission ──
      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      # ── 5. Generate debug keystore for release signing in CI ──
      - name: Generate CI signing keystore
        if: matrix.build-type == 'release'
        run: |
          mkdir -p app/keystore
          keytool -genkeypair \
            -v \
            -keystore app/keystore/ci-release.jks \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -alias ci \
            -storepass ci_release_store \
            -keypass ci_release_key \
            -dname "CN=CI, OU=CI, O=DualSpaceOBS, L=Unknown, ST=Unknown, C=US"

      # ── 6. Build APK ──
      - name: Build ${{ matrix.build-type }} APK (API ${{ matrix.api-level }})
        env:
          SIGNING_STORE_FILE: ../keystore/ci-release.jks
          SIGNING_STORE_PASSWORD: ci_release_store
          SIGNING_KEY_ALIAS: ci
          SIGNING_KEY_PASSWORD: ci_release_key
        run: |
          ./gradlew assemble${{ matrix.build-type == 'debug' && 'Debug' || 'Release' }} \
            -PcompileSdkVersion=${{ matrix.api-level }} \
            -PtargetSdkVersion=${{ matrix.api-level }} \
            --no-daemon \
            --stacktrace

      # ── 7. Prepare artifact name ──
      - name: Prepare APK artifact
        id: artifact
        run: |
          FIND_CMD="find app/build/outputs/apk/${{ matrix.build-type }} -name '*.apk' -type f"
          APK_PATH=$(eval "$FIND_CMD" | head -n1)
          if [ -z "$APK_PATH" ]; then
            echo "ERROR: No APK found for ${{ matrix.build-type }}"
            exit 1
          fi
          APK_NAME=$(basename "$APK_PATH")
          ARTIFACT_NAME="DualSpaceOBS-${{ matrix.build-type }}-api${{ matrix.api-level }}-${{ github.sha_short }}.apk"
          cp "$APK_PATH" "$ARTIFACT_NAME"
          echo "path=$ARTIFACT_NAME" >> "$GITHUB_OUTPUT"
          echo "Built: $ARTIFACT_NAME ($APK_NAME)"
        env:
          github_sha_short: ${{ github.sha }}

      # ── 8. Upload artifact ──
      - name: Upload ${{ matrix.build-type }} APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: DualSpaceOBS-${{ matrix.build-type }}-api${{ matrix.api-level }}
          path: ${{ steps.artifact.outputs.path }}
          retention-days: 30
          if-no-files-found: error

  # ──────────────────────────────────────────────
  # Job 3: Create GitHub Release (main/master only)
  # ──────────────────────────────────────────────
  release:
    name: Create GitHub Release
    needs: build
    if: |
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') &&
      (github.event_name == 'push' || github.event.inputs.create_release == 'true')
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}

      - name: Extract version from build.gradle
        id: version
        run: |
          # Try app/build.gradle first, fall back to root build.gradle
          GRADLE_FILE="app/build.gradle"
          if [ ! -f "$GRADLE_FILE" ]; then
            GRADLE_FILE="build.gradle"
          fi

          # Extract versionName (supports both single-quoted and double-quoted values)
          VERSION=$(grep -oP 'versionName\s+["\047]\K[^"\047]+' "$GRADLE_FILE" | head -n1 || true)

          if [ -z "$VERSION" ]; then
            VERSION=$(grep -oP 'version\s+["\047]\K[^"\047]+' "$GRADLE_FILE" | head -n1 || true)
          fi

          if [ -z "$VERSION" ]; then
            echo "::warning::Could not extract version from $GRADLE_FILE, using timestamp"
            VERSION="v0.0.0-$(date +%Y%m%d%H%M%S)"
          fi

          # Ensure 'v' prefix for tags
          TAG_NAME="v${VERSION#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG_NAME" >> "$GITHUB_OUTPUT"
          echo "Extracted version: $VERSION, tag: $TAG_NAME"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: true

      - name: List downloaded artifacts
        run: ls -lhR artifacts/

      - name: Check if tag already exists
        id: tag-check
        run: |
          if git tag -l "${{ steps.version.outputs.tag }}" | grep -q .; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Tag ${{ steps.version.outputs.tag }} already exists, skipping release creation."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        if: steps.tag-check.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: "Release ${{ steps.version.outputs.tag }}"
          body: |
            ## DualSpaceOBS ${{ steps.version.outputs.tag }}

            **Commit:** `${{ github.sha }}`
            **Branch:** `${{ github.ref_name }}`
            **Triggered by:** ${{ github.actor }}

            ### Artifacts
            - `DualSpaceOBS-debug-api34` — Debug APK (API 34)
            - `DualSpaceOBS-release-api34` — Release APK (API 34)
            - Additional builds for API 30 and API 33 included

            ---
            *This release was automatically generated by CI.*
          files: artifacts/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
